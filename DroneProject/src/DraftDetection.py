from typing import Optional, Tuple


class DraftDetector:
    def find(self, image) -> Optional[Tuple[int, int, int, int]]:
        """Returns None if there are no draft marks in image, otherwise returns x, y, width, height of the marks."""
        pass
